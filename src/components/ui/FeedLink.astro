<a class="feed-link" href="/feeds" aria-label="Subscribe with RSS" title="Subscribe with RSS">
  <svg
    class="feed-icon"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="1.75"
    stroke-linecap="round"
    aria-hidden="true"
  >
    <path d="M5 12a7 7 0 0 1 7 7"></path>
    <path d="M5 5.5A13.5 13.5 0 0 1 18.5 19"></path>
    <circle cx="5.4" cy="18.6" r="1.15" fill="currentColor" stroke="none"></circle>
  </svg>
</a>

<style>
  /* Mirrors .theme-toggle so the two sit as a matched pair. */
  .feed-link {
    display: grid;
    place-items: center;
    width: 2.75rem;
    height: 2.75rem;
    padding: 0;
    border-radius: 0.375rem;
    color: var(--text-primary);
    text-decoration: none;
    transition:
      background-color 160ms cubic-bezier(0.23, 1, 0.32, 1),
      transform 120ms cubic-bezier(0.23, 1, 0.32, 1);
  }

  .feed-link:hover {
    background-color: var(--surface-hover);
  }

  .feed-link:active {
    transform: scale(0.96);
  }

  .feed-link:focus-visible {
    outline: 2px solid var(--text-primary);
    outline-offset: 2px;
  }

  .feed-icon {
    width: 1.125rem;
    height: 1.125rem;
  }

  @media (prefers-reduced-motion: reduce) {
    .feed-link {
      transition: none;
    }
  }
</style>

<div class="gradient-mask" aria-hidden="true">
  <slot />
</div>

<style>
  .gradient-mask {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 6rem;
    z-index: 99;
    pointer-events: none;
    background: linear-gradient(to bottom, var(--bg) 0%, transparent 100%);
    mask-image: linear-gradient(
      to bottom,
      black 0%,
      rgba(0, 0, 0, 0.8) 20%,
      rgba(0, 0, 0, 0.6) 40%,
      rgba(0, 0, 0, 0.4) 60%,
      rgba(0, 0, 0, 0.2) 80%,
      transparent 100%
    );
    -webkit-mask-image: linear-gradient(
      to bottom,
      black 0%,
      rgba(0, 0, 0, 0.8) 20%,
      rgba(0, 0, 0, 0.6) 40%,
      rgba(0, 0, 0, 0.4) 60%,
      rgba(0, 0, 0, 0.2) 80%,
      transparent 100%
    );
    opacity: 0;
  }

  /*
    Drive fade from scroll position with no JS on supporting browsers — removes
    a global scroll listener from every page that mounts this mask.
  */
  @supports (animation-timeline: scroll()) {
    .gradient-mask {
      animation: gradient-mask-in linear both;
      animation-timeline: scroll();
      animation-range: 0px 64px;
    }
  }

  @supports not (animation-timeline: scroll()) {
    .gradient-mask {
      opacity: 1;
    }
  }

  @keyframes gradient-mask-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .gradient-mask {
      animation: none;
      opacity: 1;
    }
  }
</style>
